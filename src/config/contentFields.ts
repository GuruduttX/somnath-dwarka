/** Shared CMS field keys (model-free so client components can import safely). */
export const SHARED_FIELD_KEYS = [
  "slug",
  "title_tag",
  "meta_description",
  "h1",
  "answer_first",
  "body",
  "breadcrumb_parent",
  "canonical_override",
  "noindex",
  "status",
];
